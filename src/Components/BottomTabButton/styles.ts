import { StyleSheet } from 'react-native';

// import { colors } from '../../styles/colors';

export default StyleSheet.create({
  container: {
    height: 20,
    width: 20,
    borderWidth: 1,
  },
});
